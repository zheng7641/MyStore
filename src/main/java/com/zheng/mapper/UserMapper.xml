<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zheng.dao.UserDao">

	<resultMap type="User" id="UserResult">
		<result property="id" column="id"/>
		<result property="userName" column="userName"/>
		<result property="password" column="password"/>
		<result property="role" column="role"/>
	</resultMap>

	<!-- <select id="login" parameterType="User" resultMap="UserResult"> select 
		* from user where userName=#{userName} and password=#{password} </select> 
		<select id="getByUserName" parameterType="String" resultMap="UserResult"> 
		select * from user where userName=#{userName} </select> -->
	
	<select id="getByName" parameterType="String" resultType="user">
		select * from user where userName=#{userName}
	</select>
</mapper> 